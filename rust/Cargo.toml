[package]
name = "sol-os-rust"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]

# The kernel is freestanding and never links anything from the host.
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
debug = false

[profile.dev]
panic = "abort"
